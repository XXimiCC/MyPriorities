---
title: Лестница навыков
---

<script setup>
import { data as ladder } from '../.vitepress/data/levels.data.mts';
</script>

# Лестница навыков

> {{ ladder.count }} ступеней: пять рангов по четыре шага плюс нулевая отметка.
> Таблица собирается из кода приложения.

Как лестница выглядит на экране и как навык считает часы — на странице
[«Навыки»](/screens/skills).

## Пороги

<table tabindex="0">
  <thead>
    <tr><th>Ранг</th><th>Ступень</th><th>С какого часа</th></tr>
  </thead>
  <tbody v-for="rank in ladder.ranks" :key="rank.id">
    <tr v-for="(step, index) in rank.steps" :key="step.index">
      <td>{{ index === 0 ? rank.title : '' }}</td>
      <td>{{ step.title }}</td>
      <td>{{ step.hours === 0 ? '—' : step.hoursLabel + ' ч' }}</td>
    </tr>
  </tbody>
</table>

## Почему пороги именно такие

**Ступени внутри ранга нужны, чтобы полоса двигалась.** От «Опытного» до
«Эксперта» — девятьсот часов; без промежуточных отметок прогресс полтора года
выглядел бы как неподвижная линия.

**Новая ступень берётся примерно раз в полтора накопленного.** Поэтому движение
заметно и на тридцати часах, и на трёх тысячах.

**Пороги стоят на узнаваемых вехах.** 20 часов — «правило двадцати часов»,
1000 и 10 000 — числа, которые все слышали. Точность здесь не важна, важна
честность порядка величины: «эксперт» не должен наступать через месяц занятий.

## Как считаются часы

```
часы = вписанные вручную
     + (свёрнутые из старых месяцев + свои клики + клики привязанного приоритета)
       × цена блока
```

Три источника, и они **складываются**, а не заменяют друг друга. Из этого
следуют три вещи:

- **Привязка к приоритету ничего не переписывает** и полностью обратима: отвязали —
  навык вернулся ровно к прежнему числу.
- **Вписанные вручную часы не пересчитываются** при смене
  [цены блока](/screens/settings#цена-одного-клика). Это факт о прошлом,
  а не клики.
- **Свёрнутые часы входят в общее число** наравне с кликами. Подробнее —
  ниже.

## Свёрнутые часы

Навык, показавший 400 часов, через год покажет не меньше:
[глубина хранения не ограничена](/topics/data#глубина-хранения), и старые месяцы
никуда не деваются.

Часть часов при этом лежит не в истории, а отдельным числом навыка. Это месяцы,
свёрнутые ещё в прежнем хранилище, где историю приходилось подчищать за
горизонтом: их блоки просуммировали **перед** удалением. Сумма не потерялась,
детализация — да. Свёрнутые часы не приписаны к дням и не попадают ни в прибавку
за 30 дней, ни в темп.

Метка «до какого месяца уже свёрнуто» лежит в облаке вместе с каталогом навыков —
она защищала от двойного учёта, когда устройств несколько.

## Сравнение с приоритетами

|  | Приоритеты | Навыки |
|---|---|---|
| Горизонт | сутки, неделя, месяц | всё время, годы |
| Переключатель периода | есть | нет: вместо него темп за 30 дней |
| Полоса заполняется | относительно лидера | до следующей ступени |
| Стартовый капитал | нет | есть, вписывается вручную |
| Сколько штук | до 10 | до 24 |
| Модуль можно выключить | нет | да |

## Рядом

[Навыки](/screens/skills) · [Справочник достижений](/topics/achievements-catalogue) ·
[Пределы и лимиты](/topics/limits) · [Данные и синхронизация](/topics/data)
