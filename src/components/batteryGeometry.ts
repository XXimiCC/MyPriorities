/**
 * Геометрия батареи: доли корпуса, снятые пипеткой с references/high.jpg.
 *
 * Вынесено из BatteryIcon.tsx отдельным модулем без единого импорта, потому что
 * этими же долями рисуются иконки приложения (tools/shots/brand.mjs) и повторяет
 * их canvas-рендер обоев. Компонент тянет за собой CSS и палитру, а генератору
 * иконок нужны только числа: импорт .tsx уронил бы node на строке `import './…css'`.
 *
 * Числа заданы в долях корпуса, а не в пикселях, поэтому батарея одинаково честна
 * и в 22 пикселя в шапке, и в 512 на иконке домашнего экрана.
 */

export const BATTERY_GEOMETRY = {
  viewWidth: 112,
  viewHeight: 53,
  body: { x: 1.4, y: 1.4, w: 100, h: 50, rx: 8.5 },
  stroke: 2.4,
  nub: { w: 8.5, h: 22, rx: 3 },
  cell: { insetX: 7.5, insetY: 6.4, w: 24.8, gap: 5.3, rx: 4 },
} as const;

/** Молния поверх залитых ячеек — единственное, что отличает зарядку от полного заряда. */
export const BOLT_PATH = 'M56 9 L38.5 30.5 H49 L46 45 L63.5 23.5 H53 Z';
